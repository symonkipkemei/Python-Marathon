"""
020
Ask the user to enter
their first name and
then display the
length of their name.
"""

name = input("insert your first name: ")
nameLength = len(name)

print(f"The length of the name {name} is {nameLength}")
